;; Auto-generated. Do not edit!


(when (boundp 'timoo_msgs::timooScan)
  (if (not (find-package "TIMOO_MSGS"))
    (make-package "TIMOO_MSGS"))
  (shadow 'timooScan (find-package "TIMOO_MSGS")))
(unless (find-package "TIMOO_MSGS::TIMOOSCAN")
  (make-package "TIMOO_MSGS::TIMOOSCAN"))

(in-package "ROS")
;;//! \htmlinclude timooScan.msg.html
(if (not (find-package "STD_MSGS"))
  (ros::roseus-add-msgs "std_msgs"))


(defclass timoo_msgs::timooScan
  :super ros::object
  :slots (_header _packets ))

(defmethod timoo_msgs::timooScan
  (:init
   (&key
    ((:header __header) (instance std_msgs::Header :init))
    ((:packets __packets) ())
    )
   (send-super :init)
   (setq _header __header)
   (setq _packets __packets)
   self)
  (:header
   (&rest __header)
   (if (keywordp (car __header))
       (send* _header __header)
     (progn
       (if __header (setq _header (car __header)))
       _header)))
  (:packets
   (&rest __packets)
   (if (keywordp (car __packets))
       (send* _packets __packets)
     (progn
       (if __packets (setq _packets (car __packets)))
       _packets)))
  (:serialization-length
   ()
   (+
    ;; std_msgs/Header _header
    (send _header :serialization-length)
    ;; timoo_msgs/timooPacket[] _packets
    (apply #'+ (send-all _packets :serialization-length)) 4
    ))
  (:serialize
   (&optional strm)
   (let ((s (if strm strm
              (make-string-output-stream (send self :serialization-length)))))
     ;; std_msgs/Header _header
       (send _header :serialize s)
     ;; timoo_msgs/timooPacket[] _packets
     (write-long (length _packets) s)
     (dolist (elem _packets)
       (send elem :serialize s)
       )
     ;;
     (if (null strm) (get-output-stream-string s))))
  (:deserialize
   (buf &optional (ptr- 0))
   ;; std_msgs/Header _header
     (send _header :deserialize buf ptr-) (incf ptr- (send _header :serialization-length))
   ;; timoo_msgs/timooPacket[] _packets
   (let (n)
     (setq n (sys::peek buf ptr- :integer)) (incf ptr- 4)
     (setq _packets (let (r) (dotimes (i n) (push (instance timoo_msgs::timooPacket :init) r)) r))
     (dolist (elem- _packets)
     (send elem- :deserialize buf ptr-) (incf ptr- (send elem- :serialization-length))
     ))
   ;;
   self)
  )

(setf (get timoo_msgs::timooScan :md5sum-) "577be07a7422add7a083c04c69c02d07")
(setf (get timoo_msgs::timooScan :datatype-) "timoo_msgs/timooScan")
(setf (get timoo_msgs::timooScan :definition-)
      "# timoo LIDAR scan packets.

Header           header         # standard ROS message header
timooPacket[] packets        # vector of raw packets

================================================================================
MSG: std_msgs/Header
# Standard metadata for higher-level stamped data types.
# This is generally used to communicate timestamped data 
# in a particular coordinate frame.
# 
# sequence ID: consecutively increasing ID 
uint32 seq
#Two-integer timestamp that is expressed as:
# * stamp.sec: seconds (stamp_secs) since epoch (in Python the variable is called 'secs')
# * stamp.nsec: nanoseconds since stamp_secs (in Python the variable is called 'nsecs')
# time-handling sugar is provided by the client library
time stamp
#Frame this data is associated with
string frame_id

================================================================================
MSG: timoo_msgs/timooPacket
# Raw timoo LIDAR packet.

time stamp              # packet timestamp
uint8[1206] data        # packet contents
time data_stamp       # packet timestamp[year-month-day-hour form DISOP]
bool gps_status         # gps status from DISOP
")



(provide :timoo_msgs/timooScan "577be07a7422add7a083c04c69c02d07")


